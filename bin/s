#!/bin/sh

. /etc/rc.conf.local

for i in $pkg_scripts; do
    if [ -x /etc/rc.d/$i ]; then
        echo -n "$i: "
        sudo /etc/rc.d/$i check && echo Service running || echo Service stopped
    else
        echo $i: No such service installed
    fi
done

